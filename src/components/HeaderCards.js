import React from 'react';

const HeaderCards = ({title}) => {
    return (
        <div className="section-header">
            <h2 className="section-titulo">{title}</h2>
        </div>
    )
}

export default HeaderCards;


